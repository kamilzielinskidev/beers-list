import { createStylesMap } from '../../styles/theme';

export const styles = createStylesMap({
  paper: {
    p: 2,
  },
  container: {
    flexDirection: 'column',
  },
  title: {
    fontWeight: 800,
  },
  searchContainer: {
    mt: 2,
  },
});
